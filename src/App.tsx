// Placeholder — see CHECKPOINTS.md for the build order.
// Checkpoint 1 replaces this with the actual <Canvas> scene.
function App() {
  return (
    <div style={{ width: "100vw", height: "100vh", background: "#000", display: "flex", alignItems: "center", justifyContent: "center", color: "#0ff", fontFamily: "monospace" }}>
      3D GATEWAY — scaffold ready, see CHECKPOINTS.md
    </div>
  );
}

export default App;
